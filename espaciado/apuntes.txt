Para definir un espaciado entre COLUMNAS usamos => 'colums-gap: valor;'
Para definir un espaciado entre FILAS usamos => 'row-gap: valor;'
Para definir FILAS y COLUMNAS en una sola línea de código usamos => 'gap: FILAS(valor) COLUMNAS(valor);'